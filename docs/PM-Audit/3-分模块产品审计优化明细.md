# 分模块产品审计优化明细：AllMall AI Commerce Ops

> 审计标准：PM-Audit-Skill v3 | 基线版本：2026-07-10-v5 | 表格固定字段：7 列

---

## 经营总览 Dashboard

| 所属产品模块 | 原始产品基线内容 | 产品优化/场景新增/规则统一内容 | 关联影响业务模块 | 原有产品问题 | 优化价值 | 用户体验收益 |
|-------------|-----------------|---------------------------|-----------------|-------------|---------|-------------|
| Dashboard - 价值感知 | Dashboard 仅展示运营指标（GMV/订单/ROI），无 AI 带来的量化价值 | 新增「Agent 今日成果」三列卡片：节省时间（小时）、增收（$）、处理事务（件），底部智能洞察提示 | Agent 中心、财务台账 | 用户无法直观感知 AI Agent 创造的具体价值，产品 ROI 不可量化 | 回答"这个产品帮我省了多少时间、赚了多少钱"的核心问题，提升续费意愿 | 登录后 3 秒内看到 AI 为他创造的价值，建立产品信心 |
| Dashboard - 试用转化 | 无试用引导和升级触发 | 新增试用横幅：显示剩余天数、已用店铺数/上限、Agent 调用量/上限、待解锁高级 Agent 数，附带「升级套餐」按钮 | 财务台账 | Free 用户不知道试用何时结束，没有升级动机 | 明确试用截止线和用量约束，在恰当时机引导升级 | 用户清楚了解当前套餐状态和使用余量，减少被迫中断的负面体验 |
| Dashboard - 新手引导 | 无店铺时显示「尚未连接店铺」提示，有店铺无 Agent 时无差异化引导 | 自适应状态条分三种状态：无店铺→引导连接，有店铺无 Agent→引导开通，齐全→显示 Agent 运行概览 | 快速配置 | 不同状态的用户看到相同的 Dashboard，引导针对性不足 | 根据用户所处阶段自动切换 Dashboard 内容，缩短从注册到激活的时间 | 新用户无需探索就知道下一步该做什么 |
| Dashboard - 经营合并 | Dashboard 和经营中心（/operations）展示相似数据，用户困惑「该看哪个」 | /operations 重定向到 /dashboard，Dashboard 改为多 Tab（运营总览/Agent 效率/趋势分析） | 路由、导航菜单 | 两个页面展示重叠的 GMV/订单/广告数据，信息架构冗余 | 消除功能重叠，统一为单一数据入口，减少用户认知负担 | 用户只需一个页面就能看到所有经营数据 |

## Agent 中心 - Agent 列表

| 所属产品模块 | 原始产品基线内容 | 产品优化/场景新增/规则统一内容 | 关联影响业务模块 | 原有产品问题 | 优化价值 | 用户体验收益 |
|-------------|-----------------|---------------------------|-----------------|-------------|---------|-------------|
| Agent 列表 - 运行时可见性 | 列表仅展示 Agent 名称、描述、开关状态，无运行状态信息 | 新增三列：活跃任务数（可点击跳转）、成功率（迷你进度条）、最近执行（相对时间） | Agent 配置页、任务系统 | 用户不知道 Agent 是否真的在工作、运行得怎么样 | 从「配置面板」升级为「运营仪表盘」，用户可实时监控 Agent 运行状态 | 无需点进 Agent 详情就能了解运行健康状况 |
| Agent 列表 - 风险控制区分 | risk_control 与其他运营 Agent 混在一起，无法感知其为安全型 Agent | 添加「系统守护」橙色 Tag 标识，流程图上单独展示为「全链路护栏」 | 全局安全感知 | 用户可能忽略风险控制的重要性，将其视为普通开关 | 明确风险控制的安全属性，提升用户对产品安全能力的认知 | 一眼区分运营 Agent 和安全 Agent，决策更清晰 |
| Agent 列表 - 流程图动态化 | 运行逻辑图是静态硬编码 SVG，不反映 Agent 启用/禁用状态 | 节点根据 enabled 状态显示 ●/○ 指示器，禁用的连接线变灰，添加「已启用 X/15」汇总栏 | 无 | 图示和实际状态不一致，用户可能误以为禁用的 Agent 仍在运行 | 流程图成为可靠的状态面板，而非仅是宣传图 | 用户看到的就是实际运行的，消除信息不一致疑虑 |
| Agent 列表 - 商业锁定 | 所有 Agent 均可自由开关，无套餐限制 | Free/Starter 套餐的 Premium Agent 显示金色锁 Tag，Switch 置灰不可点击，悬停显示「升级解锁」 | 财务台账 | 用户可用全部功能但不知道需要付费，没有升级动机 | 明确付费能力边界，驱动免费用户向付费转化 | 用户清楚知道哪些功能需要升级才能用，决策有依据 |

## Agent 中心 - Agent 配置

| 所属产品模块 | 原始产品基线内容 | 产品优化/场景新增/规则统一内容 | 关联影响业务模块 | 原有产品问题 | 优化价值 | 用户体验收益 |
|-------------|-----------------|---------------------------|-----------------|-------------|---------|-------------|
| Agent 配置 - login_bootstrap | 店铺保活 Agent 有 4 处条件屏蔽，隐藏了操作指南、日志、活跃任务、空状态按钮 | 移除全部条件屏蔽，操作指南/日志/任务列表对所有 Agent 统一可见，空状态按钮根据 Agent 类型显示差异化文案 | 任务系统 | 店铺保活是最基础的 Agent，隐藏其运行状态导致用户无法排查「为什么其他 Agent 不工作」 | 统一所有 Agent 的信息展示，消除特殊处理造成的认知不一致 | 用户在保活 Agent 页面也能看到完整的运行信息和操作入口 |
| Agent 配置 - Cron 解析 | 触发条件直接展示 Cron 表达式（如 `0 */6 * * *`），非技术人员无法理解 | 添加 cronToHuman() 解析函数，显示紫色 Tag「每 6 小时」，原始 Cron 作为副标题灰显 | 无 | Cron 表达式对电商卖家来说是天书，无法判断执行频率是否合理 | 用户直观了解 Agent 执行间隔，可自行判断是否需要调整 | 降低了产品的技术门槛，让非技术卖家也能理解 Agent 行为 |
| Agent 配置 - 策略配置数据流 | 策略配置组件通过 `agent.strategyConfig.xxx = v` 直接修改对象属性，绕过 React 状态管理 | 全部改为 `queryClient.setQueryData(['agent', agentType], ...)` 不可变更新 | 全部策略配置组件 | 直接 mutate 导致 UI 不更新、缓存不一致 | 确保策略修改后 UI 即时反映变更，缓存与展示一致 | 配置更改即时生效，不会出现「改了但没反应」的情况 |

## 快速配置

| 所属产品模块 | 原始产品基线内容 | 产品优化/场景新增/规则统一内容 | 关联影响业务模块 | 原有产品问题 | 优化价值 | 用户体验收益 |
|-------------|-----------------|---------------------------|-----------------|-------------|---------|-------------|
| 快速配置 - 信息架构 | 全局 Agent 场景开通、店铺列表、运营参数配置三个层级的内容混在一个页面，配置面板从属蓝色左边框 | 添加 Steps 进度条（开通 Agent → 配置参数 → 开始运营），配置面板改为独立 Card，去掉从属视觉线 | 店铺管理、Agent 列表 | 用户不理解为什么先在全局选 Agent 然后又在店铺配参数，两个层级的空间关系混乱 | Steps 让用户清楚知道自己在哪个阶段，独立 Card 让每块内容边界清晰 | 新用户按步骤走完即可完成配置，不迷茫 |
| 快速配置 - 用户引导 | 无操作步骤提示，无进度感知 | 添加 Steps 引导组件，当前步骤高亮，未选中店铺时 Step=0 | 无 | 用户不知道配置流程有几步、现在在干什么 | 降低 Setup 流程的认知负荷，提升完成率 | 用户全程有进度感知，不会中途放弃 |

## 导航与全局

| 所属产品模块 | 原始产品基线内容 | 产品优化/场景新增/规则统一内容 | 关联影响业务模块 | 原有产品问题 | 优化价值 | 用户体验收益 |
|-------------|-----------------|---------------------------|-----------------|-------------|---------|-------------|
| 全局导航 | 12 个一级菜单项（Dashboard、异常、审批、订单、商品、Agent、模型、经营、审计、财务、说明、设置），不符合电商运营工作流 | 重组为 5 个一级菜单：经营总览、订单管理、商品管理、Agent 中心（含异常/审批子项）、设置（含 7 个子项），旧路由全部保留重定向 | 全部页面 | 12 个菜单项超出认知负荷，排序不按使用频率 | 高频操作一步可达，低频操作收进设置，降低导航成本 | 用户按工作流自然找到功能，不用在菜单里来回翻找 |
| 全局国际化 | 多个页面存在中文硬编码而未提取到 i18n（Dashboard 6 处、SetupConfig 7 处、AgentList 3 处、AppShell 19 处、Members 1 处） | 全部提取到 i18n 字典，新增 40+ 键值对（中英文各 20+），覆盖 nav.*、dashboard.*、agent.*、setup.*、app.*、settings.* 命名空间 | 全部页面 | 英文模式下部分按钮和文案仍显示中文，国际化不完整 | 中英文切换后全站文案一致，Demo 展示更专业 | 英文用户看到的全是英文，不会出现「中英混杂」的界面 |

## 空状态与反馈

| 所属产品模块 | 原始产品基线内容 | 产品优化/场景新增/规则统一内容 | 关联影响业务模块 | 原有产品问题 | 优化价值 | 用户体验收益 |
|-------------|-----------------|---------------------------|-----------------|-------------|---------|-------------|
| 通知设置 | 通知列表为空时 List 组件仅显示空白区域 | 列表添加 locale.emptyText + EmptyState 组件，提示「暂无数据」 | 无 | 用户看到空白区域不知道是该页面的正常状态还是出错了 | 空数据状态是明确的、有引导的，而非令人困惑的空白 | 用户知道「这里没有数据」而非「这里坏掉了」 |

---

## v4 新增审计项

### 成员管理 - 角色体系

| 所属产品模块 | 原始产品基线内容 | 产品优化/场景新增/规则统一内容 | 关联影响业务模块 | 原有产品问题 | 优化价值 | 用户体验收益 |
|-------------|-----------------|---------------------------|-----------------|-------------|---------|-------------|
| 成员管理 - Finance 角色 | product-design.md 定义 6 个角色（含 Finance：「仅查看财务台账，管理订阅」），代码仅实现 5 个角色 | ① domain.ts Member.role 类型新增 `'Finance'`；② MembersSettingsPage roleOptions 新增 Finance 选项；③ i18n 新增 `member.role.Finance` 中英文翻译键；④ 权限矩阵中 Finance 仅可访问财务台账模块 | domain.ts、MembersSettingsPage、i18n.tsx、mockData | 产品设计与代码实现不一致，缺少专门的财务角色，导致「谁管理订阅」职责不明确 | 角色体系与产品设计对齐，为财务人员提供专用角色，职责清晰 | 财务人员只需查看财务台账，不会被其他模块干扰，降低误操作风险 |
| 成员管理 - 邀请 Modal 国际化 | MembersSettingsPage 邀请 Modal 的 roleOptions 和 Form.Item label 均为中文硬编码（"管理员"/"运营"/"审批人"/"观察者"/"邮箱"/"角色"） | roleOptions 的 label 改为 `t('member.role.Admin')` 等 i18n 调用，Form.Item label 改为 `t('settings.email')` 和 `t('settings.role')`，placeholder 改为 i18n key | i18n.tsx（新增 member.role.* 与 settings.* 翻译键） | P013「全部硬编码中文提取到 i18n」优化遗漏了此处的 6 处中文硬编码 | 邀请 Modal 中英文切换一致，与全局国际化标准统一 | 英文用户邀请成员时看到的全是英文，不会出现中英混杂 |

### 产品设计文档 - Agent 完整性

| 所属产品模块 | 原始产品基线内容 | 产品优化/场景新增/规则统一内容 | 关联影响业务模块 | 原有产品问题 | 优化价值 | 用户体验收益 |
|-------------|-----------------|---------------------------|-----------------|-------------|---------|-------------|
| product-design.md - Agent 遗漏 | 文档第 3.3.1 节 Agent 体系全貌表格仅收录 13 个 Agent，代码实际实现 15 个 | 补全 `live_stream_ops`（直播运营）和 `promotion_campaign`（促销管理）两个 Agent 的详细设计行：名称、层级、配置项、审批要求、依赖关系、功能说明 | AgentListPage、i18n agent.*、SetupConfigPage Agent 场景映射 | 产品设计文档与代码实现不一致，新成员看文档会遗漏两个 Agent 的设计信息 | 文档与代码对齐，确保产品设计是代码的可靠依据 | 所有角色（产品、开发、测试）看到一致的 Agent 列表 |

### 代码清理 - 孤儿组件

| 所属产品模块 | 原始产品基线内容 | 产品优化/场景新增/规则统一内容 | 关联影响业务模块 | 原有产品问题 | 优化价值 | 用户体验收益 |
|-------------|-----------------|---------------------------|-----------------|-------------|---------|-------------|
| 代码清理 - OperationsCenterPage | 经营中心合并到 Dashboard 后，router.tsx 已将 /operations 重定向到 /dashboard，但 OperationsCenterPage.tsx 组件文件仍存在（约 200 行代码） | 删除 OperationsCenterPage.tsx 及相关 import，确保无残留引用 | router.tsx（确认无其他 import） | 孤儿代码增加维护成本，新人可能误修改已废弃的组件 | 减少代码库复杂度，防止误用废弃组件 | 无直接用户体验影响，但减少未来因误改导致的潜在 Bug |

---

## v5 新增审计项：逐 Agent 产品审计

### Agent #3：商品上架（product_launch）

| 所属产品模块 | 原始产品基线内容 | 产品优化/场景新增/规则统一内容 | 关联影响业务模块 | 原有产品问题 | 优化价值 | 用户体验收益 |
|-------------|-----------------|---------------------------|-----------------|-------------|---------|-------------|
| Agent - 商品上架配置对象缺失 | product_launch 的「定价规则」配置散落在 executionParams 中（defaultCategory、targetMarket），没有独立的 strategyConfig 对象 | ✅ **已修复**：为 product_launch 新增 strategyConfig.productLaunchConfig（defaultCategory、targetMarket），executionParams 清空 | AgentStrategyConfigSection、i18n | executionParams 和 strategyConfig 分裂，Agent 配置页无法为 product_launch 展示策略配置卡片 | 统一所有 Agent 的配置存放规则，减少特殊处理逻辑 | 在 Agent 详情页看到与其他 Agent 一致的策略配置区域 |

### Agent #5：广告投放（ads_optimizer）

| 所属产品模块 | 原始产品基线内容 | 产品优化/场景新增/规则统一内容 | 关联影响业务模块 | 原有产品问题 | 优化价值 | 用户体验收益 |
|-------------|-----------------|---------------------------|-----------------|-------------|---------|-------------|
| Agent - 广告投放配置分散 | targetROI 在 executionParams 中存储，adSpendBudget 在 strategyConfig 中存储，两者不同源 | ✅ **已修复**：targetROI、lookbackDays 迁移到 strategyConfig.adSpendBudget（新增 targetROI、lookbackDays 字段） | AgentStrategyConfigSection、BasicStrategySections | ROI 目标和预算上限是两个密切相关的配置，分开存储导致用户需要在两个不同位置配置 | 关联配置集中管理，修改 ROI 时自动提示预算是否需要调整 | 在一个面板完成广告投放全部配置，无需来回切换 |
| Agent - A/B 测试结果缺失 | A/B 测试创建后仅通过日志查看结果，无专门的对比分析页面 | ✅ **已实现**：在 Agent 配置页内置任务「A/B 测试」点击后打开 ABTestComparisonModal，展示全维度指标对比、置信度、胜出结论 | AgentConfigPage、ABTestComparisonModal、agentConfigMockData | 用户只看日志无法直观判断 A/B 测试效果 | 提供数据驱动的决策依据，让用户理解 Agent 为何做出某个投放决策 | 看到清晰的 A/B 对比数据和胜出原因，信任 Agent 的判断 |

### Agent #7：直播运营（live_stream_ops）

| 所属产品模块 | 原始产品基线内容 | 产品优化/场景新增/规则统一内容 | 关联影响业务模块 | 原有产品问题 | 优化价值 | 用户体验收益 |
|-------------|-----------------|---------------------------|-----------------|-------------|---------|-------------|
| Agent - 直播运营依赖标注错误 | product-design.md 原版 Agent 表格标注直播运营依赖为「无」，但 agentMockData 中 dependsOn: ['product_launch'] | ✅ **已修复**：product-design.md 中直播运营的依赖关系已修正为「商品上架」 | product-design.md、依赖流程图 | 用户看文档以为无依赖可直接开通，但代码实际要求先开商品上架 | 文档与代码对齐，避免用户开通时发现依赖不满足的困惑 | 开通前就知道需要先开商品上架，减少挫败感 |

### Agent #9：评价管理（review_manager）

| 所属产品模块 | 原始产品基线内容 | 产品优化/场景新增/规则统一内容 | 关联影响业务模块 | 原有产品问题 | 优化价值 | 用户体验收益 |
|-------------|-----------------|---------------------------|-----------------|-------------|---------|-------------|
| Agent - 评价管理配置对象缺失 | autoReplyThreshold 和 replyTone 存储在 executionParams 中，没有 strategyConfig 对象 | ✅ **已修复**：新增 reviewConfig strategyConfig 类型（autoReplyThreshold=2、replyTone=专业友好），executionParams 清空 | domain.ts、AgentStrategyConfigSection、i18n | 与 strategyConfig 设计的其他 Agent 不一致，且默认阈值 3 星偏高 | 策略配置统一管理，默认阈值降至 2 星减少不必要的自动回复 | 中评（3星）不被自动回复打扰，仅差评（1-2星）自动处理 |

### Agent #10：客服消息（customer_service）

| 所属产品模块 | 原始产品基线内容 | 产品优化/场景新增/规则统一内容 | 关联影响业务模块 | 原有产品问题 | 优化价值 | 用户体验收益 |
|-------------|-----------------|---------------------------|-----------------|-------------|---------|-------------|
| Agent - 客服消息配置对象缺失 | autoReplyEnabled 和 escalateKeywords 存储在 executionParams 中，没有 strategyConfig 对象 | ✅ **已修复**：新增 csConfig strategyConfig 类型（autoReplyEnabled=true、escalateKeywords），executionParams 清空 | domain.ts、AgentStrategyConfigSection、i18n | executionParams 中配置项不统一，且缺少工作时间外模式和延迟回复功能 | 策略配置统一管理，后续可扩展 offHoursMode 和 replyDelayEnabled | 所有 Agent 的配置都在「策略配置」区域，操作习惯一致 |

### Agent #11：售后处理（after_sales）

| 所属产品模块 | 原始产品基线内容 | 产品优化/场景新增/规则统一内容 | 关联影响业务模块 | 原有产品问题 | 优化价值 | 用户体验收益 |
|-------------|-----------------|---------------------------|-----------------|-------------|---------|-------------|
| Agent - 退货地址必填校验缺失 | returnAddress 默认值为「请填写退货地址」，未填写时 Agent 仍可能自动通过退货 | 首次进入售后处理 Agent 详情页时弹窗强提醒填写退货地址；returnAddress 为空时 Agent 不自动通过退货（改为生成审批） | AgentConfigPage、afterSalesConfig | 用户可能忘记填写退货地址，导致退货商品寄到错误地址 | 防止因退货地址缺失导致的运营事故 | 收到明确的填写提醒，不会因漏填造成损失 |

### Agent #13：库存预警（inventory_alert）

| 所属产品模块 | 原始产品基线内容 | 产品优化/场景新增/规则统一内容 | 关联影响业务模块 | 原有产品问题 | 优化价值 | 用户体验收益 |
|-------------|-----------------|---------------------------|-----------------|-------------|---------|-------------|
| Agent - 自动补货开关默认值风险 | autoReplenishEnabled 默认为 true，首次开通库存预警即启用自动补货 | 默认值改为 false，首次开启时弹窗说明「开启后将自动生成采购订单，请确认」 | inventoryConfig、库存预警配置 UI | 用户可能误开启自动补货，系统自动下采购单超出预期 | 防止用户在不了解功能的情况下自动下单造成损失 | 明确告知自动补货的影响，用户主动选择而非被动接受 |

### Agent #15：财务对账（finance_audit）

| 所属产品模块 | 原始产品基线内容 | 产品优化/场景新增/规则统一内容 | 关联影响业务模块 | 原有产品问题 | 优化价值 | 用户体验收益 |
|-------------|-----------------|---------------------------|-----------------|-------------|---------|-------------|
| Agent - 对账审批流程不合理 | 对账报告需 Owner「审批」，但报告本身是信息产物，需要的是「确认已阅」而非「通过/驳回」 | 改为「Owner 确认」机制：报告生成 → Owner 查看差异项 → 点击「确认」→ 标记为已确认并存档 | AgentConfigPage、approvalStrategy | 对账报告是信息输出而非操作审批，用审批通过/驳回语义不对 | 正确的业务流程对应正确的交互模式，避免用户困惑 | 对账确认只需一键，不用走审批流程 |

### 全局 Agent 配置一致性

| 所属产品模块 | 原始产品基线内容 | 产品优化/场景新增/规则统一内容 | 关联影响业务模块 | 原有产品问题 | 优化价值 | 用户体验收益 |
|-------------|-----------------|---------------------------|-----------------|-------------|---------|-------------|
| Agent - executionParams 迁移 | 部分 Agent 的配置散落在 executionParams 中（login_bootstrap 的 notifyChannels、product_launch 的 category/targetMarket、review_manager 的 autoReplyThreshold/replyTone、customer_service 的 autoReplyEnabled/escalateKeywords），而其他 Agent 使用 strategyConfig | ✅ **已修复**：所有 4 个 Agent 的策略性配置统一迁移到 strategyConfig（新增 bootstrapConfig/productLaunchConfig
